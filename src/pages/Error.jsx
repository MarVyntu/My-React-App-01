import React from 'react';

const Error = () => {
    return (
        <div>
            <h1 style={{color: 'red'}}>
                Ups... You have landed on a non-existent page!
            </h1>
        </div>
    );
};

export default Error;
